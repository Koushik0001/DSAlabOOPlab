#include<iostream>

using namespace std;
int main()
{
    float a,b;
    cout << "Enter the two numbers : ";
    cin >> a >> b;
    if (a>b)
        cout << a;
    else
        cout << b;
    return 0;
}